markdown
## 🆕 Новая система конфигурации

Проект перешел на новую систему конфигурации с использованием YAML/JSON файлов.

### Структура конфигурации
config/
├── modules.yaml # Списки модулей
├── dates.json # Праздники и рабочие выходные
├── coordinators.yaml # Координаторы и ведущие координаторы
└── professions.yaml # Профессии и блоки

text

### Миграция со старой системы

1. Установите зависимость:
   ```bash
   pip install pyyaml
Запустите скрипт миграции:

bash
python scripts/migrate_to_new_config.py
Проверьте конфигурацию:

bash
python scripts/validate_config.py
Управление конфигурацией через приложение
В приложении доступен раздел "Управление конфигурацией" для редактирования:

Модулей (дипломные и самозакрепление)

Координаторов

Дат (праздники и рабочие выходные)

Профессий и блоков

Программный доступ
python
from utils.config_manager import ConfigManager

# Получение конфигурации
config_manager = ConfigManager()
config_data = config_manager.data

# Использование данных
diploma_modules = config_data.diploma_modules
holidays = config_data.holidays
block_to_coordinators = config_data.block_to_lead_coordinators

# Сохранение изменений
config_manager.save_modules(new_diploma_modules, new_self_assignment_modules)
Обратная совместимость
Старый код продолжит работать через адаптер в utils/config.py:

python
from utils.config import DIPLOMA_MODULES, SELF_ASSIGNMENT_MODULES
Все изменения автоматически синхронизируются между старой и новой системами.

text

## **Шаг 6: Инструкция по применению**

### **Полный процесс внедрения:**

1. **Склонируйте или обновите проект:**
   ```bash
   cd work_analysis
Создайте новую структуру каталогов:

bash
mkdir -p config scripts
Создайте все необходимые файлы (копируйте код выше)

Установите зависимость:

bash
pip install pyyaml
Запустите миграцию:

bash
python scripts/migrate_to_new_config.py
Проверьте конфигурацию:

bash
python scripts/validate_config.py
Запустите приложение для тестирования:

bash
streamlit run app.py
Если все работает, можете удалить старый config.py:

bash
mv config.py config_old_backup.py  # Сначала создайте резервную копию